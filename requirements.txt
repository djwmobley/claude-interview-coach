# Required: for DOCX generation (tools/md_to_docx.py, tools/cheatsheet_to_docx.py, tools/cover_letter_to_docx.py)
python-docx>=1.1

# Optional: for PDF generation (tools/md_to_pdf.py)
markdown>=3.5
xhtml2pdf>=0.2.13

# Optional: for PDF text extraction (tools/convert_pdfs.py)
pypdf>=4.0
